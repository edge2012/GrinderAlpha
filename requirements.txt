# GrinderAlpha 展示仓库 — 分层依赖
# 核心引擎（investment/ + 顶层引擎）：零第三方依赖，纯 Python 标准库。
# 数据获取走 urllib（腾讯/Yahoo），估值走 akshare。

# ── 回测 / 数值计算 ──
numpy>=1.24
scipy>=1.10
pandas>=2.0

# ── 估值数据（akshare 兜底）──
akshare>=1.12

# ── AI 辩论层（investment/debate_engine/）──
pydantic>=2.0
langchain-openai>=0.1
langchain-core>=0.2
